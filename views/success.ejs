<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Submission Successful</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #e0f2f1;
    }

   .success-section {
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      padding-top: 4rem;
    }

    .success-container {
      background: #fff;
      padding: 30px;
      border-radius: 15px;
      width: 100%;
      max-width: 600px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      text-align: center;
      word-wrap: break-word;
     overflow-wrap: break-word;
     white-space: normal;
      animation: slideUp 0.8s ease-out;
    } 
    @keyframes slideUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h1 {
      color: #00695c;
      margin-bottom: 10px;
    }

    p {
      color: #333;
      margin-bottom: 15px;
      font-size: 16px;
    }
  


    .submission-details {
      text-align: left;
      margin-top: 20px;
      padding: 20px;
      background: #f9f9f9;
      border-radius: 10px;
    }

    .submission-details p {
      margin-bottom: 12px;
      font-size: 15px;
    }

   

   
    .image-preview {
  display: flex;
  justify-content: center;
  margin-bottom: 20px;
}

.image-preview img {
  width: 100%;
  max-width: 300px;
  height: 180px;
  border-radius: 10px;
  object-fit: cover;
  transition: transform 0.3s ease;
}

.image-preview img:hover {
  transform: scale(1.05);
}

    .back-button {
      display: inline-block;
      margin-top: 25px;
      padding: 12px 20px;
      background: #00695c;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-size: 16px;
      transition: background 0.3s;
    }

    .back-button:hover {
      background: #01453d;
    }

    @media (max-width: 700px) {
      .success-container {
        padding: 20px;
      }
      .submission-details {
        padding: 15px;
      }
    }
  </style>

</head>

<body>

  <%- include('partials/navbar') %>

  <section class="success-section">
    <div class="success-container">
      <h1>🎉 Submission Successful!</h1>
      <p>Thank you for submitting your startup. Here are your details:</p>

      <div class="submission-details">
        <div class="image-preview">
          <img src="/uploads/<%= submission.image %>" alt="Startup Image">
        </div>
        <p><strong>Full Name:</strong> <%= submission.fullName %></p>
        <p><strong>Email:</strong> <%= submission.email %></p>
        <p><strong>Phone Number:</strong> <%= submission.phone || "N/A" %></p>
        <p><strong>Address:</strong> <%= submission.address || "N/A" %></p>
        <p><strong>Startup Name:</strong> <%= submission.startupName %></p>
        <p><strong>Website:</strong> <a href="<%= submission.startupWebsite %>" target="_blank"><%= submission.startupWebsite %></a></p>
        <p><strong>Stage:</strong> <%= submission.stage %></p>
        <p><strong>Pitch:</strong> <%= submission.pitch %></p>
        <p><strong>Team Size:</strong> <%= submission.teamSize || "N/A" %></p>
        <p><strong>Team Members:</strong> <%= submission.teamMembers || "N/A" %></p>
        
       
      </div>

      <a href="/" class="back-button">Submit Another</a>
    </div>
  </section>

  <%- include('partials/footer') %>

  <script>
    // Show a success toast
    window.onload = function() {
      const toast = document.createElement('div');
      toast.innerText = "✅ Form Submitted Successfully!";
      toast.style.position = 'fixed';
      toast.style.top = '20px';
      toast.style.right = '20px';
      toast.style.backgroundColor = '#4caf50';
      toast.style.color = 'white';
      toast.style.padding = '10px 20px';
      toast.style.borderRadius = '5px';
      toast.style.boxShadow = '0 2px 5px rgba(0,0,0,0.3)';
      toast.style.zIndex = '1000';
      document.body.appendChild(toast);

      setTimeout(() => {
        toast.remove();
      }, 5000);
    }
  </script>

</body>
</html>
